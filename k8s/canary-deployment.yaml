apiVersion: apps/v1
kind: Deployment
metadata:
  name: chimera-agent-canary
  labels:
    app: chimera-agent
    component: ai-agent
    deployment: canary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chimera-agent
      deployment: canary
  template:
    metadata:
      labels:
        app: chimera-agent
        component: ai-agent
        deployment: canary
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      nodeSelector:
        accelerator: nvidia-tesla-k80  # or appropriate GPU selector
      tolerations:
      - key: nvidia.com/gpu
        operator: Exists
        effect: NoSchedule
      containers:
      - name: agent
        image: project-chimera/agent:latest
        ports:
        - containerPort: 8080
          name: http
        - containerPort: 8080
          name: metrics
        env:
        - name: REDIS_URL
          value: "redis://redis-service:6379"
        - name: NATS_URL
          value: "nats://nats-service:4222"
        - name: POSTGRES_URL
          valueFrom:
            secretKeyRef:
              name: chimera-secrets
              key: postgres-url
        - name: MINIO_ENDPOINT
          value: "minio-service:9000"
        - name: MINIO_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: chimera-secrets
              key: minio-access-key
        - name: MINIO_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: chimera-secrets
              key: minio-secret-key
        - name: AGENT_ID
          value: "intent-parser-canary"
        - name: MODEL_PATH
          value: "/models/gemma-3-270m-q4.safetensors"
        - name: ADAPTER_PATH
          value: "/models/adapter.safetensors"
        - name: BATCH_WINDOW_MS
          value: "15"
        - name: MAX_TOKENS
          value: "512"
        - name: CUDA_VISIBLE_DEVICES
          value: "0"
        resources:
          requests:
            nvidia.com/gpu: 1
            memory: "8Gi"
            cpu: "2000m"
          limits:
            nvidia.com/gpu: 1
            memory: "12Gi"
            cpu: "4000m"
        volumeMounts:
        - name: models-volume
          mountPath: /models
          readOnly: true
        - name: artifacts-volume
          mountPath: /artifacts
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
      volumes:
      - name: models-volume
        persistentVolumeClaim:
          claimName: models-pvc
      - name: artifacts-volume
        persistentVolumeClaim:
          claimName: artifacts-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: chimera-agent-canary-service
  labels:
    app: chimera-agent
    deployment: canary
spec:
  selector:
    app: chimera-agent
    deployment: canary
  ports:
  - name: http
    port: 80
    targetPort: 8080
  - name: metrics
    port: 9090
    targetPort: 8080
  type: ClusterIP